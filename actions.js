import { request, get, post, postFile, url, wsUrl } from "./lib/client/actions.js"

const Actions = {
  request,
  get,
  post,
  postFile,
  url,
  wsUrl
}

export default Actions
export { request, get, post, postFile, url, wsUrl }