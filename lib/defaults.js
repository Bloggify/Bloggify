module.exports = {

    // The bloggify directory (`.bloggify`)
    bloggify: ".bloggify"

    // The config file: `.bloggify/index.json`
  , config: "index"

    // Where to store the plugins
  , plugins: "plugins"

    // Where to store the plugin configs
  , pluginConfigs: "plugin-configs"
};
